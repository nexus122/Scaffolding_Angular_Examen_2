import { ImageConcatPipe } from './image-concat.pipe';

describe('ImageConcatPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageConcatPipe();
    expect(pipe).toBeTruthy();
  });
});
